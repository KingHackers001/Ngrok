# Ngrok
Ngrok nos permite exponer a internet una URL generada dinámicamente, la cual apunta a un servicio web que se está recuperando en nuestra máquina local.
#INSTALACIÓN
$ apt update && apt upgrade -y
$ termux-setup-storage
$ pkg install git -y
$ git clone https://github.com/
